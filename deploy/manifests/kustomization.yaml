apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml

# Common labels applied to all resources
commonLabels:
  app: k3s-troubleshooter
