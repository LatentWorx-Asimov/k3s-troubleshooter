apiVersion: v1
kind: Service
metadata:
  name: k3s-troubleshooter-service
  labels:
    app: k3s-troubleshooter
spec:
  selector:
    app: k3s-troubleshooter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
